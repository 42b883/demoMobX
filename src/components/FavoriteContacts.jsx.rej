diff a/src/components/FavoriteContacts.jsx b/src/components/FavoriteContacts.jsx	(rejected hunks)
@@ -1,3 +1,2 @@
-import React, { Component } from 'react';
-import FavoriteContactItem from './FavoriteContactItem.jsx';
-
+import React, { Component } from "react";
+import FavoriteContactItem from "./FavoriteContactItem.jsx";
@@ -6,34 +5,13 @@ class FavoriteContacts extends Component {
-    render() {
-        let favTitle = null;
-        if (this.props.contactsF.length) {
-            favTitle= (
-                <div>
-                     <h4 className="blue-grey-text">Favorite Contacts</h4>
-                     <br/>
-                </div>
-            )
-        }
-        let x;
-        let favContactList = this.props.contactsF.length ? (
-            this.props.contactsF.map(favorite => {
-                return (
-                    <div  key={favorite.id}>
-                    <FavoriteContactItem 
-                    idF={favorite.id}
-                    contactsF={this.props.contactsF}
-                    nameF={favorite.name}
-                    emailF={favorite.email}
-                    usernameF={favorite.username}
-                    websiteF={favorite.website}
-                    phoneF={favorite.phone}
-                    hometownF={favorite.address.city}
-                    renderFavF={this.props.renderFav}
-                    />
-                    </div>
-                )
-            })        
-
-        ) : (
-            <div className="center">Favorite contacts list is empty</div>
-        )
-        
+  render() {
+    let favTitle = null;
+    if (this.props.contactsF.length) {
+      favTitle = (
+        <div>
+          <h4 className="blue-grey-text">Favorite Contacts</h4>
+          <br />
+        </div>
+      );
+    }
+    let x;
+    let favContactList = this.props.contactsF.length ? (
+      this.props.contactsF.map(favorite => {
@@ -41,6 +19,13 @@ class FavoriteContacts extends Component {
-            
-            <div>
-            {favTitle}
-            {favContactList}
-            <br/><br/>
-            </div>
+          <div key={favorite.id}>
+            <FavoriteContactItem
+              idF={favorite.id}
+              contactsF={this.props.contactsF}
+              nameF={favorite.name}
+              emailF={favorite.email}
+              usernameF={favorite.username}
+              websiteF={favorite.website}
+              phoneF={favorite.phone}
+              hometownF={favorite.address.city}
+              renderFavF={this.props.renderFav}
+            />
+          </div>
@@ -48 +33,14 @@ class FavoriteContacts extends Component {
-    }
+      })
+    ) : (
+      <div className="center">Favorite contacts list is empty</div>
+    );
+
+    return (
+      <div>
+        {favTitle}
+        {favContactList}
+        <br />
+        <br />
+      </div>
+    );
+  }
@@ -51 +49 @@ class FavoriteContacts extends Component {
-export default FavoriteContacts
+export default FavoriteContacts;
